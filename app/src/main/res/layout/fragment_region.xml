<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical">

    <TextView
        android:id="@+id/rate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginBottom="10dp"
        android:text="@string/select_button"
        android:textAlignment="textEnd"
        android:textColor="@color/textGrayDark"
        android:textSize="18sp" />

    <View
        android:id="@+id/line"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginBottom="20dp"
        android:background="@color/lineLightGray" />

    <com.aigestudio.wheelpicker.WheelPicker
        android:id="@+id/wheel_picker"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:wheel_curved="true"
        app:wheel_indicator="true"
        app:wheel_indicator_color="@color/lineLightGray"
        app:wheel_indicator_size="1dp"
        app:wheel_item_space="20dp"
        app:wheel_item_text_color="@color/textGrayLight"
        app:wheel_item_text_size="18dp"
        app:wheel_selected_item_text_color="@color/textGrayDark"
        app:wheel_visible_item_count="5" />

</LinearLayout>

